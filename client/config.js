const API_URL = 'http://collab-ui-server-1.us-west-2.elasticbeanstalk.com';

const config = {
  MENUS_URL: `${API_URL}/menu`,
  COMPONENTS_URL: `${API_URL}/components`,
  PAGES_URL: `${API_URL}/pages`,
};

export default config;
